{
  "name": "aisa",
  "version": "1.0.2",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node -r dotenv/config --loader ts-node/esm src/index.ts",
    "type-check": "tsc --noEmit",
    "build:ts": "tsc",
    "build:tsup": "tsup",
    "build:assets": "copyfiles -u 1 src/**/*.ejs src/**/*.json dist/",
    "build": "npm run build:tsup && npm run build:assets",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "start": "node -r dotenv/config dist/index.js",
    "pb": "npm version patch && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/ejs": "^3.1.5",
    "@types/node": "^24.10.1",
    "copyfiles": "^2.4.1",
    "rimraf": "^6.1.2",
    "ts-node": "^10.9.2",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.1",
    "dotenv": "^17.2.3",
    "ejs": "^3.1.10",
    "nanoid": "^5.1.6",
    "openai": "^6.9.1",
    "zod": "^3.25.76"
  },
  "bin": {
    "audit": "./bin/cli.js"
  }
}
